import { atom } from 'jotai/vanilla';

export const showCreateSpaceAtom = atom<boolean>(false);
export const showAgentModalAtom = atom<boolean>(false);
export const showSearchAtom = atom<boolean>(false);
