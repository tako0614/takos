# Takos v0.9.0 — Installable App No-User Clean Cut

> このページでわかること: v0.9.0 リリースの変更内容と確認手順。

Release date: 2026-05-13

## Summary

- Release candidate commit: the commit tagged `v0.9.0`
- Production deployment id: not deployed by this release
- Release owner: Release owner / operator
- Operator: operator account plane, reference distribution `takosumi-cloud`
- Release window: no-user clean cut for the Takos 1.x Installable App Model
  cleanup

This is the single clean release for the Takos-owned obsolete account, billing,
credential, and AppInstallation-owned direct deploy cleanup. Takos has no
existing public users or external operators in this pre-GA line, so there is no
user data, integration, or tenant fleet to carry over. No public-user carry-over
path exists for this release.

## What Changed

- Takos remains a product running on Takosumi, not a platform/account-plane
  owner.
- Takos app stays an OIDC consumer of the operator account plane.
- App ownership, billing, grants, bindings, launch tokens, and runtime mode are
  handled through the AppInstallation ledger.
- AppInstallation-owned app lifecycle deploys use GitOps deploy intent and the
  Installable App Model.
- Takos-owned obsolete account, billing, credential, and direct deploy surfaces
  are removed or verified absent from current product code.
- The Takosumi kernel `POST /v1/deployments` remains the canonical unmanaged
  deploy entry point and is not part of this Takos product cleanup.

## No-User Clean-Cut Assertion

- Operator assertion: Takos has no existing public users, external operators,
  user data, integrations, or tenant fleet for this release line.
- Current scope: obsolete pre-GA Takos product-owned account, billing,
  credential, and direct deploy surfaces are removed or verified absent.
- Public-user action: none. If external users are discovered later, handle that
  through an operator incident/runbook rather than by restoring obsolete Takos
  ownership.

Affected Takos product-owned obsolete surfaces:

- Takos-owned OAuth/OIDC issuer endpoints: `/oauth/*`, `/api/public/v1/oauth/*`,
  `/api/oauth/*`, and OAuth provider discovery.
- Takos app-local credential and PAT issuance surfaces that conflict with
  operator account-plane issued bearer/PAT credentials.
- Takos app-owned billing owner surfaces that conflict with the operator
  account-plane BillingPort.
- Takos app public/proxy direct deploy routes that bypass GitOps deploy intent
  for AppInstallation-owned app lifecycle operations.
- Agent or managed-skill direct deploy affordances such as
  `group_deployment_snapshot_deploy_from_repo`.

## Route Evidence

The release evidence for the current source state is:

```sh
cd takos
deno task validate:retired-route-removal

cd ..
rg -n "group_deployment_snapshot_deploy_from_repo|group_deployment_snapshot\\.deploy_from_repo|deploy_from_repo" \
  takos/app takos/agent -g '!node_modules' -g '!target'
```

`validate:retired-route-removal` passed 44 route/evidence tests, including the retired
route expectations, and the direct deploy affordance scan returned no hits for
current Takos product code. This scan does not target takosumi kernel deploy
APIs.

## Account Model

No public-user action exists for this no-user clean cut. The operator-facing reference remains:
[`docs/operator/account-model.md`](../operator/account-model.md)

No current public-user action exists. Current operator contract invariants:

- Keep Takos configured as an OIDC consumer of the operator account plane.
- Keep account, credential, upstream IdP, billing, and AppInstallation lifecycle
  ownership in Takosumi Accounts or a contract-compatible operator distribution.
- Keep AppInstallation-owned deploy automation on GitOps deploy intent.
- Keep direct kernel deploy use on the unmanaged `POST /v1/deployments` path
  only.

## Validation Evidence

- Release gate: `cd takos && deno task release-gate` passed for the clean-cut
  release candidate commit with `dirty=false`.
- Retired-route removal validator: `cd takos && deno task validate:retired-route-removal`
  passed 44 route/evidence tests.
- Release promotion validator:
  `cd takos && deno task validate:release-promotion` passed.
- Agent docs validation: `cd takos && deno task lint:agent-docs` passed.
- Docs build: covered by `deno task release-gate` via `lint:docs`.
- Architecture checks: `cd takos && deno task validate:architecture` passed and
  ecosystem root `deno task check:architecture` passed.
- No-user clean-cut ledger:
  `takos-private/docs/operations/no-user-clean-cut-evidence.md`.

## Rollback Plan

No production deployment is performed by this release. If a deployment later
references this release and must be rolled back, use the operator rollback SOP
and keep the operator account-plane AppInstallation ledger intact. Do not roll
back by restoring Takos-owned OAuth issuer, billing, PAT, or
AppInstallation-owned direct deploy ownership as current contract.

## Communication

- Customer notice: this GitHub release.
- Support: GitHub issues on `tako0614/takos` or the operator support channel.
- Known limitation: this release is a no-user clean cut. If external users are
  discovered later, handle them through an operator incident/runbook rather than
  reintroducing retired Takos-owned obsolete surfaces.
