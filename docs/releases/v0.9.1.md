# Takos v0.9.1 - Installable App Migration Deprecation

Release date: 2026-05-13

## Summary

- Release candidate commit: the commit tagged `v0.9.1`
- Production deployment id: not deployed by this deprecation release
- Release owner: Release owner / operator
- Operator: operator account plane, reference distribution `takosumi-cloud`
- Release window: deprecation window for the Takos 1.x Installable App Model
  migration

This is the public deprecation release for the migration away from Takos-owned
legacy account, billing, credential, and AppInstallation-owned direct deploy
surfaces. It follows the `v0.9.0` announcement release and adds a dated warning
artifact for operators and integrations. It does not complete the ROADMAP
community-announcement gate by itself; the removal release with separate public
evidence is still required.

## Deprecation Warning

The following Takos product-owned legacy surfaces are deprecated for integrations
that should move to the Installable App Model:

- Takos-owned OAuth/OIDC issuer endpoints: `/oauth/*`, `/api/public/v1/oauth/*`,
  `/api/oauth/*`, and OAuth provider discovery.
- Takos app-local credential and PAT issuance surfaces that conflict with
  operator account-plane issued bearer/PAT credentials.
- Takos app-owned billing owner surfaces that conflict with the operator
  account-plane BillingPort.
- Takos app public/proxy direct deploy routes that bypass GitOps deploy intent
  for AppInstallation-owned app lifecycle operations.
- Agent or managed-skill direct deploy affordances such as
  `group_deployment_snapshot_deploy_from_repo`.

The Takosumi kernel `POST /v1/deployments` remains the canonical unmanaged
deploy entry point and is not part of this Takos product legacy removal.

## Migration Guide

Operator migration guide:
[`docs/operator/account-migration.md`](../operator/account-migration.md)

Required operator action:

- Keep Takos configured as an OIDC consumer of the operator account plane.
- Keep account, credential, upstream IdP, billing, and AppInstallation lifecycle
  ownership in Takosumi Accounts or a contract-compatible operator distribution.
- Keep AppInstallation-owned deploy automation on GitOps deploy intent.
- Keep direct kernel deploy use on the unmanaged `POST /v1/deployments` path
  only.

Required user or integration action:

- Stop depending on Takos-owned OAuth issuer URLs.
- Move Takos app-local credentials or PAT automation to operator-issued
  bearer/PAT credentials.
- Move AppInstallation-owned deploy automation to GitOps deploy intent.

## Validation Evidence

- Release gate: `cd takos && deno task release-gate` passed for the commit
  tagged `v0.9.1`.
- Release promotion validator:
  `cd takos && deno task validate:release-promotion` passed.
- Docs build: covered by `deno task release-gate` via `lint:docs`.
- Agent docs validation: `cd takos && deno task lint:agent-docs` passed.
- Migration-window ledger:
  `takos-private/docs/operations/migration-window-evidence.md`.
- Ecosystem sign-off:
  `roadmap-1x-deprecation-2026-05-13`.

## Rollback Plan

No production deployment is performed by this deprecation release. If a
deployment later references this release and must be rolled back, use the
operator rollback SOP and keep the operator account-plane AppInstallation ledger
intact. Do not roll back by restoring Takos-owned OAuth issuer, billing, or PAT
ownership as current contract.

## Communication

- Customer deprecation notice: this GitHub release.
- Support: GitHub issues on `tako0614/takos` or the operator support channel.
- Known limitation: the migration window is not complete until the removal
  release is published with a distinct Takos tag and takos-ecosystem sign-off
  release.
