# takos-docs

`takos-docs` is a legacy app name. The current Office app is [`takos-office`](/platform/takos-office), where
Docs is the `/docs` surface of one removable Capsule app.

The legacy `/takos-docs/` storage folder remains for data compatibility.
