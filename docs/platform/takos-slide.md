# takos-slide

`takos-slide` is a legacy app name. The current Office app is [`takos-office`](/platform/takos-office), where
Slide is the `/slide` surface of one removable Capsule app.

The legacy `/takos-slide/` storage folder remains for data compatibility.
