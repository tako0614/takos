# takos-excel

`takos-excel` is a legacy app name. The current Office app is [`takos-office`](/platform/takos-office), where
Sheet is the `/sheet` surface of one removable Capsule app.

The legacy `/takos-excel/` storage folder remains for data compatibility.
