apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "takos.fullname" . }}-takosumi-log-worker
  labels:
    {{- include "takos.labels" . | nindent 4 }}
    app.kubernetes.io/component: takosumi-log-worker
spec:
  replicas: {{ .Values.paasLogWorker.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/component: takosumi-log-worker
      {{- include "takos.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "takos.labels" . | nindent 8 }}
        app.kubernetes.io/component: takosumi-log-worker
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: takosumi-log-worker
          image: "{{ .Values.images.control.repository }}:{{ .Values.images.control.tag }}"
          imagePullPolicy: {{ .Values.images.control.pullPolicy }}
          command: ["deno", "task", "start"]
          ports:
            - name: http
              containerPort: {{ .Values.paasLogWorker.port }}
              protocol: TCP
          env:
            - name: PORT
              value: {{ .Values.paasLogWorker.port | quote }}
            - name: PROXY_BASE_URL
              value: {{ include "takos.paasLogWorkerUrl" . | quote }}
            - name: CONTROL_RPC_BASE_URL
              value: {{ include "takos.paasLogWorkerUrl" . | quote }}
            - name: TAKOSUMI_RUNTIME_AGENT_URL
              value: {{ include "takos.paasRuntimeAgentUrl" . | quote }}
            - name: TAKOS_LOCAL_EXECUTOR_URL
              value: {{ include "takos.executorUrl" . | quote }}
          envFrom:
            - configMapRef:
                name: {{ include "takos.fullname" . }}-global
            - secretRef:
                name: {{ .Values.secrets.existingSecrets.platform | default (printf "%s-platform" (include "takos.fullname" .)) }}
            - secretRef:
                name: {{ .Values.secrets.existingSecrets.auth | default (printf "%s-auth" (include "takos.fullname" .)) }}
            - secretRef:
                name: {{ .Values.secrets.existingSecrets.llm | default (printf "%s-llm" (include "takos.fullname" .)) }}
                optional: true
          livenessProbe:
            httpGet:
              path: /livez
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.paasLogWorker.resources | nindent 12 }}
