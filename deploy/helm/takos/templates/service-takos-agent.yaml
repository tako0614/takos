apiVersion: v1
kind: Service
metadata:
  name: {{ include "takos.fullname" . }}-takos-agent
  labels:
    {{- include "takos.labels" . | nindent 4 }}
    takos.io/service-id: takos-agent
    app.kubernetes.io/component: takos-agent
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.services.takosAgent.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/component: takos-agent
    {{- include "takos.selectorLabels" . | nindent 4 }}
