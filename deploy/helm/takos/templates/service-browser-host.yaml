apiVersion: v1
kind: Service
metadata:
  name: {{ include "takos.fullname" . }}-browser-host
  labels:
    {{- include "takos.labels" . | nindent 4 }}
    app.kubernetes.io/component: browser-host
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.browserHost.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/component: browser-host
    {{- include "takos.selectorLabels" . | nindent 4 }}
