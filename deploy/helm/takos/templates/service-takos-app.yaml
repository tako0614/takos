apiVersion: v1
kind: Service
metadata:
  name: {{ include "takos.fullname" . }}-takos-app
  labels:
    {{- include "takos.labels" . | nindent 4 }}
    takos.io/service-id: takos-app
    app.kubernetes.io/component: takos-app
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.services.takosApp.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/component: takos-app
    {{- include "takos.selectorLabels" . | nindent 4 }}
