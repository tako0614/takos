{{- if .Values.secrets.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "takos.fullname" . }}-redis
  labels:
    {{- include "takos.labels" . | nindent 4 }}
type: Opaque
data:
  REDIS_URL: {{ include "takos.redisUrl" . | b64enc | quote }}
{{- end }}
