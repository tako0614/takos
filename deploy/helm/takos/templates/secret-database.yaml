{{- if .Values.secrets.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "takos.fullname" . }}-database
  labels:
    {{- include "takos.labels" . | nindent 4 }}
type: Opaque
data:
  DATABASE_URL: {{ include "takos.databaseUrl" . | b64enc | quote }}
{{- end }}
