apiVersion: v1
kind: Service
metadata:
  name: {{ include "takos.fullname" . }}-runtime
  labels:
    {{- include "takos.labels" . | nindent 4 }}
    app.kubernetes.io/component: runtime
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.runtime.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/component: runtime
    {{- include "takos.selectorLabels" . | nindent 4 }}
